# ⚖️ Conflict Resolution & Contradiction Analysis (`research/CONFLICT-RESOLUTION.md`)

> **Executive Objective**: Systematically resolve every technical divergence, conflicting threshold, and architectural contradiction across the three research reports with empirical justification.

---

## 1. Contradiction Resolution Matrix

| # | Contradiction Domain | Report 1 View | Report 2 View | Report 3 View | Empirical Evaluation & Trade-offs | Canonical Resolution for DebugForge | Decision Type |
| :-: | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **1** | **BRT Iteration Counts** | $N \ge 20$ iterations under jitter | $10/10$ runs for standard; $N=50$ for concurrency | $N=10$ pristine sandbox runs | High iteration counts ($N=50$) for simple deterministic null dereferences waste 3 minutes of test runtime; low counts ($N=1$) miss race conditions. | **Staged Execution**: $N=3$ for deterministic crashes, $N=10$ for async operations, $N=20-50$ strictly under schedule perturbation for race conditions. | **STAGED APPROACH** |
| **2** | **Verification Architecture** | Cross-family model routing + mutation testing | Asymmetric 3-Lock (Deterministic, Mutation, Cross-Model) | N-Version 3-Agent Consensus (Semantic, Execution, Security) | Relying strictly on 3 distinct API keys causes immediate harness failure when an operator has only one key configured. | **Deterministic-First + Adaptive Cross-Family**: Lock 1 & 2 are deterministic (tests + anti-gaming). Lock 3 uses cross-family if configured, or role-isolated asymmetric prompt if single-key. | **CONDITIONAL APPROACH** |
| **3** | **Sandbox Confinement** | Dual gVisor + Landlock microVMs | Sandlock (Landlock LSM + seccomp-bpf) | Daytona OCI microVM containers | Landlock and seccomp are Linux-kernel specific and fail to compile on Windows/macOS developer machines. | **Daytona Isolated MicroVMs**: Use Daytona SDK for remote container boundaries; maintain cross-platform host portability. | **CHOOSE DAYTONA** |
| **4** | **Memory Persistence Scope** | Vector DB with 7-day TTL and cryptographic salting | Physical partition keys + SMT proof requirement | Ephemeral task memory; avoid vector memory contamination | Global vector DBs frequently retrieve stale hypotheses from past tasks, corrupting unrelated repository repairs. | **In-Memory Task Store with Strict Task Isolation**: Ephemeral memory per task; zero cross-task state leakage without human promotion. | **CHOOSE TASK ISOLATION** |
| **5** | **RCA Slicing vs. Stack Walk** | Full bytecode dynamic backward slicing | Bytecode dynamic slicing + AutoSD mutation | Post-hoc causal DAG reconstruction | Dynamic instruction tracing (`sys.settrace`, JVMTI) causes a $50\times - 100\times$ execution slowdown, blowing past timeouts. | **Causal Provenance Engine + AST Runtime Probes**: Call-frame stack walking to decouple crash site from origin, augmented with scoped runtime probes. | **HYBRID REPLACEMENT** |
| **6** | **Supervisor Loop Strategy** | Entropy reduction score + Merkle graph | Progress Score $\mathcal{P} \le 0$ + Levenshtein cycle detection | Semantic Checkpoint Graph + $>95\%$ match breaker | Simple attempt counters fail to catch alternating edit cycles ($A \to B \to A$). | **Unified Autonomous Supervisor**: SHA-256 state hash cycle detection + 3x consecutive failure watchdog + 10-attempt cap. | **UNIFIED SYNTHESIS** |
